# Spendly Client

React + TypeScript frontend for Spendly.

## Scripts

* `npm run dev` starts the Vite development server
* `npm run build` creates a production build
* `npm run preview` serves the production build locally

## Notes

Set `VITE_API_URL` in your frontend environment so the client can reach the backend API.
